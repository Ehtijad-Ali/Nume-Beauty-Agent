# ============================================================
# NUMÉ AI Marketing Assistant — Backend Dependencies
# Phase 1.1 (Auth + CRUD + Infra, no AI yet)
# ============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Database ---
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# --- Cache / broker ---
redis==5.2.1

# --- Auth & security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Document ingestion (Phase 2.1) ---
pypdf==5.1.0
python-docx==1.1.2

# --- Vector search (Phase 2.2) ---
qdrant-client==1.12.1
fastembed==0.4.2

# --- RAG engine / LLM providers (Phase 2.3) ---
anthropic>=0.60.0
openai>=1.60.0
# Optional OCR extras — install to enable image indexing:
# pytesseract==0.3.13
# Pillow==11.1.0

# --- Logging / utilities ---
python-json-logger==3.2.1
loguru==0.7.3

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- Code quality (dev only) ---
# ruff==0.8.4
# mypy==1.14.0
