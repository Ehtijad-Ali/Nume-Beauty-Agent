"""
AI package — Phase 2 (placeholders only, not yet implemented).

This package will host all AI-related functionality once Phase 2 begins:
LLM provider clients, agent orchestration, memory stores, prompt templates
and embedding generators.

Phase 1.1 does NOT implement any AI functionality. These folders exist so
future work can land cleanly without restructuring the codebase.
"""

# Sub-packages (placeholders):
#   ai.providers    — LLM provider clients (OpenAI, Anthropic, Gemini, …)
#   ai.agents       — Agent orchestration / tool-use loops
#   ai.memory       — Short-term / long-term agent memory stores
#   ai.prompts      — Prompt template registry & renderers
#   ai.embeddings   — Embedding model wrappers
